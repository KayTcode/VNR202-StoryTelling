import{loadJSON}from'./jsonLoader.js';const root=document.querySelector('#gallery'),box=document.querySelector('#lightbox');try{const data=await loadJSON('gallery.json');root.innerHTML=data.map((x,i)=>`<article class="card" data-id="${i}" tabindex="0"><div class="gallery-art" aria-label="${x.alt}" role="img"></div><div class="card-copy"><p class="eyebrow">${x.year}</p><h2>${x.title}</h2><p>${x.caption}</p></div></article>`).join('');const open=e=>{const card=e.target.closest('.card');if(!card)return;const x=data[card.dataset.id];document.querySelector('#light-year').textContent=x.year;document.querySelector('#light-title').textContent=x.title;document.querySelector('#light-description').textContent=x.description;box.showModal()};root.addEventListener('click',open);root.addEventListener('keydown',e=>{if(e.key==='Enter')open(e)});box.querySelector('.close').onclick=()=>box.close()}catch(e){root.textContent='Không thể tải thư viện.'}
